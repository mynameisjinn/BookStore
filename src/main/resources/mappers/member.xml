<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.bookstore.repository.MemberRepository">
<!--    <select id="selectMemberAll" resultType="org.example.bookstore.vo.MemberVO">-->
<!--        SELECT *-->
<!--        FROM member;-->
<!--    </select>-->

    <select id="selectMemberAll" resultType="org.example.bookstore.vo.MemberVO">
        SELECT member_id, address, email, create_date, password, id
        FROM member;
    </select>

</mapper>